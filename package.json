{
  "name": "store-rating-platform",
  "version": "1.0.0",
  "description": "Full-stack Store Rating Web Application with React, Express, PostgreSQL, and JWT Authentication",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue.bold,green.bold\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start:backend": "npm run start --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "test:backend": "npm test --prefix backend",
    "db:setup": "npm run db:setup --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:reset": "npm run db:reset --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "react",
    "express",
    "postgresql",
    "jwt",
    "store-rating"
  ],
  "author": "",
  "license": "ISC"
}
